<%= form_for :like, class: 'form-horizontal', url: likes_path do |f| %>
	<%= f.hidden_field :user_id, value: current_user.id %>
	<%= f.hidden_field :post_id, value: post.id %>
	<%= f.submit "+", class: "btn btn-success btn-sm pull-right" %>	
<%end%>
